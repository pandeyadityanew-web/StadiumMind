# StadiumMind AI - Environment Variables Matrix

# Gemini / Google GenAI SDK Credentials
# (If omitted, the backend falls back to high-fidelity simulated agentic outputs)
GEMINI_API_KEY=your_gemini_api_key_here

# Backend Connection config
NEXT_PUBLIC_API_URL=http://localhost:8000
HOST=0.0.0.0
PORT=8000
